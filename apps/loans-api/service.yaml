apiVersion: v1
kind: Service
metadata:
  name: loans-api-service
  labels:
    app: loans-api
spec:
  type: ClusterIP
  selector:
    app: loans-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8083
